
using UnityEngine;
using UnityEditor;

namespace Hobbies {
    [CustomPropertyDrawer(typeof(#SCRIPTNAME#))]
    public class #SCRIPTNAME# : PropertyDrawer {
        void OnEnable() {

        }

        public override void OnGUI(Rect position, SerializedProperty property, GUIContent label) {
            EditorGUI.BeginProperty(position, label, property);

            // Step1: Find a property: var x = property.FindPropertyRelative("x");
            // Step2: Property's Rect: var xRect = new Rect(position.x, position.y, position.width, EditorGUIUtility.singleLineHeight);
            // Step3: Draw the property: x.boolValue = EditorGUI.Toggle(xRect, "X", x.boolValue);

            EditorGUI.EndProperty();
        }
    }
}